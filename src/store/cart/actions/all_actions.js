import {
    postCart,
    postCartGuest,
    handleGuestCart,
    removeItem,
    emptyCart,
    getCart,
    handleGuestQuantity,
    loadCartGuest,
    handleGuestDelete
} from "./cart_actions";

const allActions = {
    postCart,
    postCartGuest,
    handleGuestCart,
    emptyCart,
    getCart,
    removeItem,
    handleGuestQuantity,
    loadCartGuest,
    handleGuestDelete
};

export default allActions;